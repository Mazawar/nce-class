{
  "name": "nce-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:data": "python3 scripts/build-data.py",
    "dev": "vite",
    "build": "npm run build:data && vite build"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "pdfjs-dist": "^3.11.174"
  }
}
